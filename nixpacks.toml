# Tell Railway/Nixpacks to install both Node.js AND Python
[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["npm ci", "pip install -r requirements.txt"]

[start]
cmd = "npm run start"
