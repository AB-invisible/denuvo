/node_modules
# Compiled TypeScript output (built via `npm run build`)
/dist
# Keep environment variables out of version control
.env

/generated/prisma

# Python bytecode
__pycache__/
*.pyc

# Generated test/token zips (created at runtime)
/Generated_Tokens/

# Auto-sync log (rotates locally, no value in versioning)
/tools/sync_and_push.log

# Build-time secret injection for installer.exe (webhook URL etc.).
# Generated fresh each CI run from repo secrets — never commit.
/installer/_config.py
/installer/build/
/installer/dist/

# Local dev helper scripts
sync-dlls.bat
update-dlls.bat

# EA token tool analysis / local secrets (never commit)
/EAtoken_generator.exe_extracted/
/ea-service-test/
config.ini
scripts/extract_pyinstaller.py
scripts/extract_pyz.py

# EA magic zips (too large for GitHub; host on disk / Railway volume)
/ea-magic/*.zip
